import { TwinDirectory } from "@/components/directory/TwinDirectory";

export const metadata = {
  title: "Twin Directory | PersonaAI",
  description: "Discover and chat with AI digital twins",
};

export default function DirectoryPage() {
  return <TwinDirectory />;
}
